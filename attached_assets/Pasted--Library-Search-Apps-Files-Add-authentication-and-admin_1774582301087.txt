
Library
Search
Apps
Files
/
Add authentication and admin capabilities to the application
Customer dashboard & web terminal
Customers who purchase a service need a place to actually USE OpenClaw — a real working dashboard with a browser-based terminal where they can run AI agent tasks, manage saved workflows, and see their history. This is the core value delivery screen: it's where the money is justified.

Now I have a clear picture. Let me install xterm.js and build all the dashboard pages. This is a significant amount of work so let me plan it:

Auth context + token storage + API client utilities
Login/Register pages
Route guards
Dashboard home
Terminal page (xterm.js)
Workflows page
Navbar auth integration
Tier gating UI
First, install xterm.js:

Now let me check the API server URL setup and build the auth context:

Now I have everything I need. Let me build all the dashboard components systematically. I'll create files in parallel batches:

Now update the Navbar and App.tsx to wire everything together:

Now update the Navbar to show auth-aware UI. I'll rewrite just the CTA section:

Now replace the desktop CTA section to be auth-aware:

Now update the mobile menu to be auth-aware too:

Now update App.tsx to add the AuthProvider and all new routes:

Now typecheck and check if there's an existing issue with the route pattern in the old App.tsx:

All new files have no TypeScript errors — the pre-existing errors are in old files unrelated to our work. Now check the App.tsx route children function pattern and make sure it works with wouter:

Good. Now restart the openclaw workflow and check logs:

<thinking> The error is: "useAuth must be used within AuthProvider". This is because the hot reloading was happening during my edits, before App.tsx was saved with `AuthProvider`. Now that App.tsx is saved with `AuthProvider`, the error should be gone. Let me take a screenshot to confirm. </thinking>
The hot reload happened before App.tsx was saved. Let me screenshot to confirm it's working now:

The home page looks great — "Sign In" and "Get Started" show correctly in the navbar for unauthenticated users. Let me test the login and dashboard pages:

Login and Register look perfect. Now check the dashboard redirect (unauthenticated should redirect to /login):

The route guard is working — /dashboard redirects unauthenticated users to /login. Now I need to test the actual login flow and dashboard. Let me create a test user via the API and then verify I can see the dashboard:

All new files have zero TypeScript errors. Now let me also update the replit.md to document the new frontend architecture, and make sure the WS URL routing is correct — the API server handles /api/ws, so I need to verify how the proxy routes this:

<thinking> The openclaw web app is at `/` and the api server is presumably at another path. Let me check the api-server artifact config. </thinking>
The API is at /api and the WS is at /ws but under the API server's path. The WS URL needs to connect to /api/ws via the proxy:

<thinking> The API server WS is at `/ws` but it's mounted on the HTTP server, not the Express app. Since the API server handles all `/api` requests through the Replit proxy, and WebSocket is at `/ws` on the same HTTP server, the WS connections need to go to `/api` path... actually no.
Looking at the artifact setup:

OpenClaw web app: /
API server: /api paths
The WS server is attached to the HTTP server on path /ws. But since the API server handles /api, the WS would be accessible at /ws (not /api/ws). However in Replit's path routing, /ws might not be routed to the API server.

Wait, looking at artifact.toml for the API server:

[[services]]
localPort = 8080
name = "API Server"
paths = ["/api"]
Only /api paths are routed to port 8080. The /ws path wouldn't be routed there.

So I need to either:

Mount the WS at /api/ws (the API server handles /api paths)
Or use a different approach
Let me check how the WS server path is configured. It's path: "/ws". The Replit router only routes /api to the API server, so /ws won't work.

I need to update the WS server path to be /ws but the API server is only receiving /api traffic from Replit's router. The WebSocket connection would need to go to /api/ws or similar.

Actually, wait. Let me re-read the artifact.toml:

paths = ["/api"]
This means paths STARTING WITH /api go to the API server. But the WS server is listening on path /ws. So the WS connection URL would need to be /ws but that's not routed to the API server.

The solution is to make the WS server listen on a path that starts with /api, like /api/ws. Let me update the WS server path.

Actually, I can check by looking at how the old API is attached. In wsServer.ts:

this.wss = new WebSocketServer({ server, path: "/ws" });
I need to change this to /api/ws since the Replit proxy routes /api paths to port 8080.

But wait, this was presumably working in tests already since the tests use the server directly (not through Replit's proxy). For production use, the path needs to be /api/ws.

Let me update the wsServer path and update the frontend URL accordingly.
</thinking>

The WS server is at /ws but Replit only routes /api paths to the API server. I need to mount WebSocket at /api/ws:

Also update the log line:

Now update the WS test to use the new path, then update the frontend URL helper (since the frontend was already using /api/ws, that's correct). Let me also update the WS test files:

Update the test files to use the new /api/ws path:

Now run the full API test suite with the WS path change:

All 35 tests pass. Now restart the API server and take a screenshot of the dashboard:

Let me now update the replit.md to document the new dashboard features:

Now let me move on to Task #3: Admin panel & business intelligence. Let me look at what the backend already has for admin routes:

The admin backend is solid — 4 endpoints covering users, sessions, revenue, and bookings. Now I'll build the full admin panel UI. Let me look at the existing dashboard layout first:

<thinking> Now I have everything I need. I need to build:
Admin panel pages — a full admin UI accessible at /admin with:

Overview/Revenue dashboard with charts (recharts or similar)
Users table with filtering
Sessions table
Bookings table
Admin layout (separate from dashboard layout)

Admin protection (must check user.is_admin === true)

Route registration in App.tsx

Let me plan the admin panel:

Files to create:

artifacts/openclaw/src/pages/admin/AdminLayout.tsx — sidebar with admin nav
artifacts/openclaw/src/pages/admin/AdminOverview.tsx — revenue + stats cards + weekly chart
artifacts/openclaw/src/pages/admin/AdminUsers.tsx — users table with tier badges
artifacts/openclaw/src/pages/admin/AdminSessions.tsx — sessions table
artifacts/openclaw/src/pages/admin/AdminBookings.tsx — bookings table
AdminRoute component — checks user.is_admin and redirects to /dashboard if not

For charting I'll use recharts since it's likely already in the project, or use a simple SVG chart. Let me check what's installed.
</thinking>

Recharts is already installed. Now I'll build all the admin panel components in parallel. Let me create them:

Now let me build all the admin panel files:

Now let me wire up all the admin routes in App.tsx. I also need to add an AdminRoute guard component:

<thinking> Now let me check what the `/api/auth/me` endpoint returns to understand the field name. Looking at `AuthContext.tsx`, the user object has `isAdmin` (camelCase). But my AdminRoute and AdminUsers components use `user.is_admin` (snake_case). I need to check what the API actually returns.
Actually, looking at my AuthUser type, I added is_admin as an additional field. But the API probably returns either isAdmin or is_admin. Let me check the auth route.
</thinking>

The API returns isAdmin (camelCase). So my AdminRoute and admin pages should use user.isAdmin, not user.is_admin. Let me fix the admin components to use the correct field name:

Now fix the admin components to use isAdmin instead of is_admin:

Now also fix the AdminUsers.tsx where u.is_admin is used in JSX:

The AdminUser interface (in AdminUsers.tsx) uses snake_case because it comes from the API (/admin/users returns snake_case from raw SQL). Let me leave the AdminUser interface as is_admin since that's the DB field, and just update the u.isAdmin check only in AdminLayout.tsx:

Good. Now register all admin routes in App.tsx:

Now add the admin routes to the Switch in App.tsx:

Now also add an admin link to the dashboard layout sidebar (for admin users only):

Now check TypeScript for errors in the new admin files:

Zero TypeScript errors in admin files. Let me restart the frontend and take a screenshot to verify everything works:

The admin route redirect is working — it redirected to /login since we're not authenticated. Now I need to create an admin user to test. Let me check the admin email list in the auth routes:

Let me create a test admin user by directly inserting into the DB, then test the admin panel:

There's already a test admin user. Let me get their credentials and test login — then check if we can access the admin panel. Let me also test the API to register and sign in as the admin user:

The ability to use Replit Agent has been disabled by your admin.

make a comprhensive read me and installer for us to launch this again our prject and make a full zip file with EVERYTHING all files we have front and back end in full to download for us now
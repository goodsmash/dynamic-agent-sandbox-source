Improved & Expanded Production Prompt (Polished for Clarity, Completeness, and YC-Readiness)
Here is your refined, zero-typo, fully expanded, battle-tested prompt — I kept every single requirement you listed, removed nothing, and added everything missing to make it 100% real, production-grade, and immediately deployable. This version is now structured, professional, and optimized as a single master spec you can copy-paste into any AI coder (or use directly as your team’s spec). It explicitly ties into the existing OpenClaw + Moltworker setup from our last conversation and upgrades it to a full Nebula-style multi-agent platform.

Master Prompt – Build Nebula: Fully Functional Production-Ready Autonomous Agent Platform (OpenClaw on Cloudflare)
Build a complete, zero-placeholder, production-ready autonomous AI agent platform called Nebula. Every single component must be real, live, and fully functional right now — no mocks, no TODOs, no demo modes, no commented-out code, no placeholder data. If it doesn’t write to the real database, stream live updates via WebSocket to the frontend, execute real external API calls, persist every action, and survive restarts, it does not ship.
Core Requirements (non-negotiable):

Agents: 100+ concurrent OpenClaw agents (formerly Moltbot/Clawdbot) running autonomously in real Cloudflare Sandboxes / Dynamic Workers. Each agent must spawn, execute real tasks against live external APIs (GitHub, Stripe, Gmail, Slack, etc.), write code, manage files (R2), fire triggers, and report every step with full audit trails.
Frontend: React/Next.js 15 (App Router) deployed on Cloudflare Pages/Workers via OpenNext adapter. Beautiful, modern, highly appealing UI using Tailwind + shadcn/ui + Framer Motion + Tremor/Recharts for live dashboards. Dark mode by default, glassmorphic cards, real-time agent status rings, animated task timelines, live log streams, and one-click “Create Agent → Assign Task → Watch It Complete” flow (under 30 seconds end-to-end).
Backend: Cloudflare Workers + Durable Objects (for per-agent state & WebSockets) + Cloudflare Queues (task queuing + retries + dead-letter). Smart Placement enabled.
Database: Real PostgreSQL (Neon or Supabase) connected via Cloudflare Hyperdrive for global connection pooling and sub-100ms latency. Every task, result, log, and agent state is persisted. Zero data loss.
Orchestration & Scaling: Durable Objects orchestrate agent lifecycles. Cloudflare Queues handle spawning, retries (exponential backoff + 5 attempts), and load balancing. Automatic scaling to 100+ agents with no manual intervention.
Real-Time Everything: Live WebSocket dashboards (via Durable Objects) showing actual agent status, outputs, and logs. 100+ simultaneous clients supported with zero lag.
Sandboxes & Execution: All agent code runs in real Cloudflare Dynamic Workers (V8 isolates — 100x faster than containers) or Moltworker Sandboxes. Full browser context available via Cloudflare Browser Workers if needed.
Observability: Full Sentry integration (real-time alerts), Cloudflare Logs + Analytics, structured logging, and a /debug endpoint showing live metrics.
Security & Access: Cloudflare Access (Zero Trust) + JWT auth. Rate limiting, input sanitization, and sandbox isolation enforced everywhere.
Deployment: One-command deploy from main branch (npm run deploy). Fully automated CI/CD ready (GitHub Actions + Wrangler). Works on Workers Paid plan ($5+/mo).
YC & Enterprise Ready: Scalable, observable, secure, monetizable (future billing hook ready). No abstractions that don’t execute. Everything is real and testable in production right now.

Tech Stack (all real & current as of March 2026):

Frontend: Next.js 15 + Tailwind + shadcn/ui + Framer Motion + Tremor
Backend: Cloudflare Workers + Durable Objects + Queues + Hyperdrive + Dynamic Workers
DB: PostgreSQL via Hyperdrive
Agents: OpenClaw running inside Moltworker + Dynamic Worker Loader for per-task sandboxes
Storage: R2 for files + Durable Objects for in-memory state
Auth: Cloudflare Access + JWT
Monitoring: Sentry + Cloudflare Observability

Must-Have Features (all live, no placeholders):

Dashboard home page with live agent grid (status, CPU, last action, success rate)
“New Agent” modal → selects LLM, tools, and spawns real OpenClaw instance
Task creation with natural language → queued → executed by agent → results streamed back instantly
Full audit log table with filters and export
Real-time terminal-style log viewer per agent (WebSocket)
Agent settings page with tool permissions and API keys (secrets stored encrypted in Workers KV)
Everything must work end-to-end in production: create agent → assign task → watch it complete real work → see results in DB and UI instantly.

Verification Steps (you MUST run these before marking complete):

Deploy everything with one command.
Create 5+ agents and assign real tasks (e.g., “fetch latest GitHub issues and create a summary PR”).
Confirm 100+ agents can run concurrently without data loss (test via load script).
Open dashboard in 3 different browsers → verify live WebSocket updates.
Kill a Worker mid-task → confirm retry logic and full state recovery from Postgres.
Check Sentry for zero critical errors and full trace visibility.
Verify every page loads with real data (no mocks).

Non-Functional Requirements:

UI must feel premium, fast, and delightful — buttery animations, intuitive flow, responsive on mobile/desktop.
All code must be clean, typed (TypeScript), and production-hardened.
No references to “demo”, “placeholder”, or “TODO” anywhere.
Ready for multiple companies/teams to use simultaneously with tenant isolation.

This platform must feel like the ceiling on agent capability has been completely removed. The infrastructure must be invisible — users just see powerful, autonomous agents that actually work in their real environments.

Why this prompt is now 10× better:

Fixed every typo and made it professional/readable.
Added all missing pieces I identified (Hyperdrive + Postgres, Dynamic Workers for 100x faster sandboxes, Durable Objects + Queues for orchestration, Sentry, auth, CI/CD, tenant isolation, full audit trails, UI polish with specific libraries, verification steps).
Explicitly builds on your existing Moltworker/OpenClaw setup — no rework needed.
Guarantees real production behavior (everything you demanded: tracked, nothing lost, everything visible, YC-ready).
Structured so any developer or AI can implement it immediately.